name: Run tests
on:
  push:
    paths:
      - .github/workflows/tests.yml
      - pyproject.toml
      - pixi.lock
      - '**.py'
      - data/**
jobs:
  build:
    name: Create figures
    strategy:
      matrix:
        python-version: ["3.14"]
        os: ["ubuntu-latest"]
    runs-on: ${{ matrix.os }}
    steps:
    - name: Checkout repo
      uses: actions/checkout@v6
    - name: Set up pixi and try to create figures
      run: |
        curl -fsSL https://pixi.sh/install.sh | sh
        export PATH="$HOME/.pixi/bin:$PATH"
        pixi install
        pixi run python fig01_trends.py
        pixi run python fig02_regional_clusters.py
        pixi run python fig03_network.py
        pixi run python fig04_ff_vs_wlsq.py
        